spring.application.name=AuthApp

# MySQL config:
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/authappdb
spring.datasource.username=root
spring.datasource.password=SQL24@MyDBSrS


# Enabling automatic table creation
spring.jpa.hibernate.ddl-auto=update

# SQL queries in logs
spring.jpa.show-sql=true

# Dialect for MySQL, performance tuning
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Enable initialization of the database, runs data.sql file
# spring.sql.init.mode=always

# Log the execution of the data.sql file
# logging.level.org.springframework.jdbc.datasource.init.ScriptUtils=DEBUG



#jwt.secret=your-secret-key
#jwt.expiration=3600000